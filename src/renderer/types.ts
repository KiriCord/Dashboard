export type Mer = {
  gas: Number,
  oil: Number,
  dt: String
}

export type CharProps = {
  data: Mer[];
}

export type InfoWellProps = {
  WellId: string;
}

export type isOnlineProps = {
  isOnline: boolean;
}
